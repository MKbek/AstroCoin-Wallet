name: Build and Publish

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Publish to build branch
        env:
          PERSONAL_ACCESS_TOKEN: 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
        run: |
          git config --global user.name "Muhammadiy"
          git config --global user.email "muhammad.700@inbox.ru"
          git checkout -b build
          git add .
          git commit -m "Publish build"
          git remote set-url origin https://github.com/MKbek/AstroCoin-Wallet.git
          git push origin HEAD:build
